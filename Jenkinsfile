


pipeline {
   agent any

   stages {
      stage('Hello') {
         steps {
            echo 'Hello World'
            echo "param: ${war_file}"
            echo "param: ${params.war_file}"
            
         }
      }
   }
}
